{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "// 참고": "Supabase 대시보드 → Project Settings → Database → Connection string(Session/Direct, 포트 5432)에서 복사. 실제 비밀번호는 아래 대신 user-secrets/환경변수로 주입할 것.",
    "Default": "Host=db.YOUR-PROJECT-REF.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=YOUR-DB-PASSWORD;SSL Mode=Require;Trust Server Certificate=true"
  }
}
